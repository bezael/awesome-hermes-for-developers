# assets/

Sin assets binarios (diagramas, imágenes) en esta versión de la skill — el flujo completo se cubre
en texto en `SKILL.md` y `references/`. Si en algún momento se graba el vídeo demostración mencionado
en `MCP/README.md`, un diagrama del flujo `mcp.json` → `config.yaml` → verificación puede vivir aquí.
